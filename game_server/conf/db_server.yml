# 数据服务器配置

# 服务配置
service:
  name: DBServer
  id: 4
  ip: 0.0.0.0
  port: 8300
  max_connections: 1000
  
# 心跳配置
heartbeat:
  interval: 30
  timeout: 60
  
# 数据写入配置
data_write:
  batch_size: 100
  interval: 60
  retry_times: 3
